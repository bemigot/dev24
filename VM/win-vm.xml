<domain type="kvm">
  <name>dev24-win-test</name>
  <memory unit="GiB">4</memory>
  <vcpu>2</vcpu>
  <os firmware="efi">
    <type arch="x86_64" machine="q35">hvm</type>
    <!-- Windows 11 requires UEFI + Secure Boot; libvirt picks the matching
         OVMF Secure-Boot firmware from these feature hints (provided by the
         `ovmf` package) and allocates per-domain nvram automatically. -->
    <loader secure="yes"/>
    <firmware>
      <feature enabled="yes" name="enrolled-keys"/>
      <feature enabled="yes" name="secure-boot"/>
    </firmware>
    <boot dev="hd"/>
  </os>
  <features>
    <acpi/>
    <apic/>
    <smm state="on"/>  <!-- required for Secure Boot -->
    <hyperv mode="custom">
      <relaxed state="on"/>
      <vapic state="on"/>
      <spinlocks state="on" retries="8191"/>
    </hyperv>
  </features>
  <cpu mode="host-model"/>
  <clock offset="localtime">
    <timer name="hypervclock" present="yes"/>
  </clock>
  <devices>
    <disk type="file" device="disk">
      <driver name="qemu" type="qcow2"/>
      <!-- Placeholder path — harness.py patches this at runtime from
           OVERLAY_IMAGE. Change to wherever your images live as needed. -->
      <source file="/opt/dev24-vm/run-overlay.qcow2"/>
      <target dev="vda" bus="virtio"/>
    </disk>
    <interface type="network">
      <source network="default"/>
      <model type="virtio"/>
    </interface>
    <graphics type="spice" autoport="yes"/>
    <video>
      <model type="qxl"/>
    </video>
    <channel type="spicevmc">
      <target type="virtio" name="com.redhat.spice.0"/>
    </channel>
    <!-- Windows 11 requires TPM 2.0; emulated via swtpm -->
    <tpm model="tpm-crb">
      <backend type="emulator" version="2.0"/>
    </tpm>
  </devices>
</domain>
